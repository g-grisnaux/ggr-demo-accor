apiVersion: v1
kind: Service
metadata:
  name: graphql-bff
  namespace: demo-accor
  labels:
    app: graphql-bff
spec:
  selector:
    app: graphql-bff
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
